# dotfiles

## Installation
use something like gnu stow

### Linux

```
git clone https://github.com/oliverl-21/dotfiles ~/.dotfiles
cd ~/.dotfiles
stow -v -t ~ -d .
```
### macOS
```
brew install stow
git clone https://github.com/oliverl-21/dotfiles ~/.dotfiles
cd ~/.dotfiles
stow -v -t ~ -d .
```